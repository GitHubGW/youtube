extends ../layout.pug
include ../mixins/mixinVideo.pug

block content
  div 
    h1 home
    h2 #{sessionId}
    h3 #{hello}
    each video in videos
      +mixinVideo(video)
    else 
      li 비디오를 찾을 수 없습니다.